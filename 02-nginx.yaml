- name: installing nginx
  hosts: frontend
  become: yes

  tasks:
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present

  - name: starting nginx
    ansible.builtin.service:
      name: nginx
      state: started